name: Merge main branch to test
on:
  push:
    branches:
      - '**'
jobs:
  merge-branch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master

      - name: Luacheck
        uses: nebularg/actions-luacheck@v1
